<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_height"
        android:background="@drawable/bg_temple_lintel">

        <TextView
            android:id="@+id/tvTempleTitle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="ΞDGΞCΛSΞ"
            android:contentDescription="EdgeCase"
            android:textAppearance="@style/TitleMonolith"
            android:textSize="@dimen/header_title_size" />

        <!-- The Serpent's Eyes — service indicator (Phase 7 #1), one on each flank of the lintel.
             Shown only on the main menu; MainActivity hides them on the sub-screens. -->
        <com.dicereligion.edgecase.ServiceEyeView
            android:id="@+id/serviceEyeLeft"
            android:layout_width="34dp"
            android:layout_height="24dp"
            android:layout_gravity="start|center_vertical"
            android:layout_marginStart="16dp"
            android:visibility="gone"
            android:contentDescription="Service status" />

        <com.dicereligion.edgecase.ServiceEyeView
            android:id="@+id/serviceEyeRight"
            android:layout_width="34dp"
            android:layout_height="24dp"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="16dp"
            android:visibility="gone"
            android:contentDescription="Service status" />
    </FrameLayout>

    <!-- Meander trim under the lintel -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="@dimen/meander_trim_height"
        android:src="@drawable/ic_meander_horizontal"
        android:scaleType="fitXY"
        android:alpha="0.55"
        android:importantForAccessibility="no" />
</LinearLayout>
